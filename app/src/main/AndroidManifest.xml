<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.C4.connect4application">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connect4Application"
        >
        <activity
            android:name="com.C4.connect4application.activity.HostActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name="com.C4.connect4application.utils.MusicSoundService"/>
    </application>

</manifest>